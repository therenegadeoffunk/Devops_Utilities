# Note that traceroute6 is often installed by default instead of traceroute.

# Basic

traceroute google.com

# TCP scan

traceroute -T google.com # May need root privilege

# ICMP scan

traceroute -I google.com

# UDP with specific port scan

tracerout -U -p 53 google.com

# Skip DNS lookup

traceroute -n google.com
