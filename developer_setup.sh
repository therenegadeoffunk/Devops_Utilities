# This is a basic box setup script for ubuntu development environments
# It can be used for linux laptops or cloud-based development environments

# Install needed software

sudo apt-get install vim
sudo apt-get install virtualbox
sudo apt-get install docker.io
sudo apt-get install traceroute
sudo apt-get install nmap
sudo apt-get install mtr
sudo apt-get install vagrant
sudo apt-get install rvm
sudo apt-get install chef

# Create directory structure

mkdir ~/workspace

# TODO: Clone projects if necessary

# TODO: Setup ssh

# TODO: Security configuration

# TODO: Set up swap if necessary
